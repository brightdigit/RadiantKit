name: RadiantKit
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  RadiantKit:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
